{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "sleep 10 && php artisan config:clear && php artisan cache:clear && php artisan route:clear && php artisan view:clear && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}